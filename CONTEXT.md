# 接触表

从视频抽出截帧、排成网格、再编码成一张图。用来一眼看完一段视频。

## Language

**接触表**:
从一个视频抽出的截帧按一种版式排成的图。可以是静图，也可以是多帧动画。
_Avoid_: 截图合集, 雪碧图, 拼图

**版式**:
接触表里截帧的大小和位置。换版式不改变截帧、时间戳、参数栏各自的含义。
_Avoid_: 样式, 缩略图样式, 布局, 模板

**等大网格**:
默认版式。每一格截帧的宽和高都相同，列数和格数由命令指定。
_Avoid_: 普通网格, 均匀网格

**版式 1**:
一种不等大版式。一截是 14 格，按 4 列、5 个等高的行单位排列。左上和右下各有一格占 2 列×2 行，其余 12 格各占 1 列×1 行。正中间那一行四个都是小格。大格内部没有分割。截帧按从左到右、从上到下放入格子；扫到大格的左上角就占用它，所以右下那个大格是第 12 张，最后一行左边两格是第 13、14 张。列数固定为 4，格子像素固定，画面保住自己的比例、不裁切。另外写的列数、格子宽、格子高都不起作用，并在终端说明。能改的只有往下排几截：在这个版式下，格数参数改指截数。不写就是一截。格与格之间留 8 像素黑缝，大格把内部那条缝盖进自己里面，左右外缘留同样宽的一条，上下外缘不留。从第二截起，两截之间多一行 4 个小格，专门隔开上一截的右下大格和下一截的左上大格；这一行也是截帧，所以两截是 32 张，三截是 50 张。等大网格仍贴在一起。参数栏横跨整张图，文字左对齐，不按截重复。
_Avoid_: 样式 1, 缩略图

**一截**:
版式 1 里那一块固定的 14 格。它是往下重复的单位，不是整张接触表。从视频抽帧时，格数参数改指截数，不写就是一截；0 截不出图。多截时，截与截之间另有一行 4 个小格，不计入这一截的 14 张。`join` 按送入的张数推断截数：一截 14 张，之后每多一截加 18 张，对不上就不出图，格数参数不起作用。每一截是不同的采样时刻，不是同一截再画一遍。
_Avoid_: 一页, 一块, 一组

**大格**:
版式 1 里占 2 列×2 行的那一格截帧。它仍是一个采样时刻，不是四张画面叠在一起。边长是两个小格再加上它们之间的 8 像素缝。从视频抽帧时按大格抽出，再缩小放进小格；动画的每一帧都缩小。`join` 把每张图缩成同一个大格：高度是两个小格再加上那条缝，比例跟第一张图走，对不上就拉伸。
_Avoid_: 大图, 主图

**小格**:
版式 1 里占 1 列×1 行的那一格截帧。高度固定为 216，宽度按画面比例换算后四舍五入，不由命令指定。结果是奇数就少 1 像素；收到 0 就不出图。
_Avoid_: 小图, 普通格

**缺帧**:
该出现的截帧没有抽出来。等大网格用前一张补上。版式 1 不补，不出这张接触表。
_Avoid_: 补帧, 丢帧

**截帧**:
视频时间轴上一个采样点对应的画面，在接触表里占一格。一格可以是单帧，也可以是一小段动画。
_Avoid_: 缩略图, 帧

**时间戳**:
印在每一格截帧右下角、表示该格采样时刻的文字。字号随该格短边变化，约为短边的 16%，最大 40 像素。没有底色，靠黑色描边和右下阴影与画面分开。
_Avoid_: 标签, 字幕

**参数栏**:
接触表网格上方的一块文字，列出源视频的事实，不属于任何一格截帧。
_Avoid_: 页眉, 标题, 水印, 信息条

**简要参数**:
参数栏的默认内容，共五行：文件名，大小，分辨率与帧率，第一条视频解码器和第一条音频解码器（同一行，音频只写名字），时长。没有音轨就不写音频。
_Avoid_: 默认参数, 短参数

**完整参数**:
参数栏的加长内容：简要参数，再加上一份固定清单里探测到的其余事实。包括每一条音轨、总码率、视频码率、每条音轨的码率、声道、采样率、像素格式、容器格式、总帧数。某个文件没有的字段不写。不写创建时间、语言或编码器标签。
_Avoid_: 全部参数, 详细参数

**视频轨**:
源视频里真正的画面流。分辨率、帧率、像素格式、总帧数只描述第一条视频轨。
_Avoid_: 封面, 附件图

**封面图**:
被探测成视频流、但只是附在文件上的一张静图。它不是视频轨，参数栏不写它。
_Avoid_: 附件, 海报

**参数栏字体**:
用来画参数栏的字体。按指定文件、MiSans、系统自带中文字体的顺序找，找到即用。都没有时，参数栏改用时间戳那套字体，标签改成英文。
_Avoid_: 内嵌字体, 页眉字体

**码率**:
每秒的比特数。1000 进位，和 ffmpeg 的打印一致。跟文件大小不是同一套进位。
_Avoid_: 比特率, 码流
