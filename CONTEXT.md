# 接触表

从视频抽出截帧、排成网格、再编码成一张图。用来一眼看完一段视频。

## Language

**接触表**:
从一个视频抽出的截帧排成的网格图。可以是静图，也可以是多帧动画。
_Avoid_: 截图合集, 雪碧图, 拼图

**截帧**:
视频时间轴上一个采样点对应的画面，在接触表里占一格。一格可以是单帧，也可以是一小段动画。
_Avoid_: 缩略图, 帧

**时间戳**:
印在每一格截帧上、表示该格采样时刻的文字。
_Avoid_: 标签, 字幕

**参数栏**:
接触表网格上方的一块文字，列出源视频的事实，不属于任何一格截帧。
_Avoid_: 页眉, 标题, 水印, 信息条

**简要参数**:
参数栏的默认内容，共五行：文件名，大小，分辨率与帧率，第一条视频解码器和第一条音频解码器（同一行，音频只写名字），时长。没有音轨就不写音频。
_Avoid_: 默认参数, 短参数

**完整参数**:
参数栏的加长内容：简要参数，再加上一份固定清单里探测到的其余事实。包括每一条音轨、总码率、视频码率、每条音轨的码率、声道、采样率、像素格式、容器格式、总帧数。某个文件没有的字段不写。不写创建时间、语言或编码器标签。
_Avoid_: 全部参数, 详细参数

**视频轨**:
源视频里真正的画面流。分辨率、帧率、像素格式、总帧数只描述第一条视频轨。
_Avoid_: 封面, 附件图

**封面图**:
被探测成视频流、但只是附在文件上的一张静图。它不是视频轨，参数栏不写它。
_Avoid_: 附件, 海报

**参数栏字体**:
用来画参数栏的字体。按指定文件、MiSans、系统自带中文字体的顺序找，找到即用。都没有时，参数栏改用时间戳那套字体，标签改成英文。
_Avoid_: 内嵌字体, 页眉字体

**码率**:
每秒的比特数。1000 进位，和 ffmpeg 的打印一致。跟文件大小不是同一套进位。
_Avoid_: 比特率, 码流
